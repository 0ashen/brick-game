import { DisplayRow } from './display-row';

export type DisplayMatrix20x10 = [
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow,
  DisplayRow
];
