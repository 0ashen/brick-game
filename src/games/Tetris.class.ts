import { Game } from './Game.abstractClass';
import { BGScreen, Render } from '../Render.class';

export class Tetris extends Game {

    constructor(render: Render) {
        super(render);
    }

    public run(): void {

    }
}